---
title: My Dashboard
description: Welcome to your nsbi dashboard
---

# Getting Started

Add `.csv` or `.parquet` files to the `data/` directory, then write SQL queries here.

```sql
-- name: example
SELECT 1 as value, 'hello' as message
```

<BigValue data="example" value="value" title="Example Metric" />
