---
title: CLI Reference
description: Polaris command-line interface
---

## Commands

### `polaris dev`

Start the development server with hot reload.

```bash
polaris dev [options]
```

| Option | Default | Description |
|--------|---------|-------------|
| `--project <path>` | `.` | Project directory (must contain `pages/`) |
| `--port <port>` | `3000` | Port to listen on |

### `polaris init`

Scaffold a new Polaris project.

```bash
polaris init [name] [options]
```

| Option | Default | Description |
|--------|---------|-------------|
| `--template <name>` | `blank` | Template: `blank`, `saas-metrics`, `sales-pipeline`, `product-analytics` |

### `polaris build`

Build for production. Pre-executes queries and generates a static site.

```bash
polaris build [options]
```

| Option | Default | Description |
|--------|---------|-------------|
| `--project <path>` | `.` | Project directory |

Output goes to the directory specified in `polaris.config.ts` (default: `dist/`).

### `polaris preview`

Preview the production build locally.

```bash
polaris preview [options]
```

| Option | Default | Description |
|--------|---------|-------------|
| `--port <port>` | `4173` | Port to listen on |
